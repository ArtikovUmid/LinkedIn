import React from "react"
import { Header } from "../../shared/components/templates"
import {useParams} from "react-router-dom"
import {nav} from "../../shared/const"

export const MainPage = () => {
	return (
		<div>
			<Header/>
		</div>
	);
}

